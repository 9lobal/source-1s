// пример вызова 
// ФС_ОбщийВызовСервера.ВыгрузитьМВТВФайл(ПараметрыРасчета.МенеджерВременныхТаблиц, "C:\Users\1cbit.trushnikov\Documents\Конфигурация\ВыгрузкиМВТ\РассчитатьЗачетАвансовПолученных.xml");
Функция ВыгрузитьМВТВФайл(МВТ, Путь) Экспорт
	Если Не ТипЗнч(МВТ) = Тип("МенеджерВременныхТаблиц") Тогда
		Возврат "Неверный параметр 1: ожидается Тип(""МенеджерВременныхТаблиц"")";
	КонецЕсли;
	Структура = Новый Структура;
	Для Каждого ВременнаяТаблица Из МВТ.Таблицы Цикл
		ИмяВременнойТаблицы = ВременнаяТаблица.ПолноеИмя;
		Структура.Вставить(ИмяВременнойТаблицы, ВременнаяТаблица.ПолучитьДанные().Выгрузить());
	КонецЦикла;
	ЗаписьТекста = Новый ЗаписьТекста;
	Попытка
		ЗаписьТекста.Открыть(Путь);
		ЗаписьТекста.Записать(Сериализовать(Структура));
		ЗаписьТекста.Закрыть();
		Возврат Путь;
	Исключение
		Возврат ОписаниеОшибки()
	КонецПопытки;
КонецФункции